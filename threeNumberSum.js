function threeNumberSum(array, targetSum) {
  // sort array
  array.sort((a, b) => (a - b))
  // loop 
  
  // sum: pointer before, current number and final number.
	// sum > target, move current left
	// sum < target, move current right
	// sum === target, push tripplet
	// if current === final, final--
	
	
}
